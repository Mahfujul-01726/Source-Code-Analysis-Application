from setuptools import setup, find_packages

setup(
    name='source_code_analysis_app',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add required packages here
    ],
)
